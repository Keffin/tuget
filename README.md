# tuget

Terminal UI for browsing and managing NuGet packages in .NET projects.

## Requirements 

Requires Node 22+ and the [.NET SDK](https://dotnet.microsoft.com/download) (`dotnet` on your `PATH`)

## Adding soon

- Transitive dependency view
- Prerelease version toggle
